import fs from 'node:fs';

const swiftPath = 'plugins/criblo-native-browser/ios/Sources/CRIBrowserPlugin/CRIBrowserPlugin.swift';
const testPath = 'scripts/test-ios-longpress-bridge.mjs';

function replaceOnce(text, label, from, to) {
  const first = text.indexOf(from);
  if (first < 0) throw new Error(`${label}: source block not found`);
  if (text.indexOf(from, first + from.length) >= 0) throw new Error(`${label}: source block not unique`);
  return text.slice(0, first) + to + text.slice(first + from.length);
}

let swift = fs.readFileSync(swiftPath, 'utf8');

swift = replaceOnce(
  swift,
  'pointer compatibility state',
  `      var compatSyntheticMouseDownTarget = null;\n      var compatSyntheticMouseDownX = 0;\n      var compatSyntheticMouseDownY = 0;\n      var compatSyntheticMouseDownAt = 0;\n      var compatPressMoved = false;`,
  `      var compatSyntheticPointerDownTarget = null;\n      var compatSyntheticMouseDownTarget = null;\n      var compatSyntheticMouseDownX = 0;\n      var compatSyntheticMouseDownY = 0;\n      var compatSyntheticMouseDownAt = 0;\n      var compatPressMoved = false;\n      var suppressedIOSPointerDown = null;\n      var suppressedIOSPointerUp = null;`,
);

const oldEnsure = `      function ensureAndroidPressStart(target, x, y, touchStartedAt, sourceEvent) {\n        if (!target || !target.dispatchEvent) return;\n        // WKWebView supplies a real touch pointerdown, usually just BEFORE\n        // touchstart. Treat that as the Android pointerdown instead of creating\n        // a duplicate. What iOS does not supply on a long touch is Chrome's\n        // compatibility mousedown, so create that immediately after touchstart.\n        var pointerNearTouchStart = lastRealPointerDownTarget === target\n          && lastRealPointerDownAt > 0\n          && Math.abs(lastRealPointerDownAt - touchStartedAt) < 160;\n        if (!pointerNearTouchStart) {\n          try {\n            if (typeof PointerEvent === 'function') {\n              var pd = new PointerEvent('pointerdown', {\n                bubbles:true,cancelable:true,composed:true,\n                clientX:x,clientY:y,screenX:x,screenY:y,\n                button:0,buttons:1,pointerId:realTouchIdentifier == null ? 1 : realTouchIdentifier,\n                pointerType:'touch',isPrimary:true,width:9,height:9,pressure:1,view:window\n              });\n              if (sourceEvent) {\n                __cribloSyntheticContextEvents.add(pd);\n                __cribloSyntheticContextSources.set(pd, sourceEvent);\n              }\n              target.dispatchEvent(pd);\n              __cribloGeoDiag.syntheticPointerDowns++;\n              traceAndroidEvent('pointerdown', false);\n            }\n          } catch (_) {}\n        }\n        var mouseNearTouchStart = lastRealMouseDownTarget === target\n          && lastRealMouseDownAt > 0\n          && Math.abs(lastRealMouseDownAt - touchStartedAt) < 160;\n        if (!mouseNearTouchStart && compatSyntheticMouseDownTarget !== target) {\n          mouse(target, 'mousedown', x, y, 0, 1, sourceEvent);\n          __cribloGeoDiag.syntheticMouseDowns++;\n          traceAndroidEvent('mousedown', false);\n          compatSyntheticMouseDownTarget = target;\n          compatSyntheticMouseDownX = x;\n          compatSyntheticMouseDownY = y;\n          compatSyntheticMouseDownAt = Date.now();\n          compatPressMoved = false;\n        }\n      }`;

const newEnsure = `      function ensureAndroidPressStart(target, x, y, touchStartedAt, sourceEvent) {\n        if (!target || !target.dispatchEvent) return;\n        // iOS WKWebView emits pointerdown BEFORE touchstart. Android GeoReseaux\n        // emits touchstart first. The native iOS pointerdown is suppressed from\n        // page listeners below; replay one touch pointerdown here, immediately\n        // after the genuine touchstart has completed propagation.\n        if (compatSyntheticPointerDownTarget !== target) {\n          try {\n            if (typeof PointerEvent === 'function') {\n              var pd = new PointerEvent('pointerdown', {\n                bubbles:true,cancelable:true,composed:true,\n                clientX:x,clientY:y,screenX:x,screenY:y,\n                button:0,buttons:1,pointerId:realTouchIdentifier == null ? 1 : realTouchIdentifier,\n                pointerType:'touch',isPrimary:true,width:9,height:9,pressure:1,view:window\n              });\n              if (sourceEvent) {\n                __cribloSyntheticContextEvents.add(pd);\n                __cribloSyntheticContextSources.set(pd, sourceEvent);\n              }\n              target.dispatchEvent(pd);\n              __cribloGeoDiag.syntheticPointerDowns++;\n              traceAndroidEvent('pointerdown', !!(sourceEvent && sourceEvent.isTrusted));\n              compatSyntheticPointerDownTarget = target;\n            }\n          } catch (_) {}\n        }\n\n        var mouseNearTouchStart = lastRealMouseDownTarget === target\n          && lastRealMouseDownAt > 0\n          && Math.abs(lastRealMouseDownAt - touchStartedAt) < 160;\n        if (!mouseNearTouchStart && compatSyntheticMouseDownTarget !== target) {\n          mouse(target, 'mousedown', x, y, 0, 1, sourceEvent);\n          __cribloGeoDiag.syntheticMouseDowns++;\n          traceAndroidEvent('mousedown', !!(sourceEvent && sourceEvent.isTrusted));\n          compatSyntheticMouseDownTarget = target;\n          compatSyntheticMouseDownX = x;\n          compatSyntheticMouseDownY = y;\n          compatSyntheticMouseDownAt = Date.now();\n          compatPressMoved = false;\n        }\n      }\n\n      function finishAndroidPointerRelease(request) {\n        if (!request || !request.target) return;\n        var target = request.target;\n        if (compatSyntheticPointerDownTarget !== target) return;\n        var x = request.x == null ? compatSyntheticMouseDownX : request.x;\n        var y = request.y == null ? compatSyntheticMouseDownY : request.y;\n        var source = request.releaseEvent || request.sourceEvent || null;\n        try {\n          if (typeof PointerEvent === 'function') {\n            var pu = new PointerEvent('pointerup', {\n              bubbles:true,cancelable:true,composed:true,\n              clientX:x,clientY:y,screenX:x,screenY:y,\n              button:0,buttons:0,pointerId:request.pointerId == null ? 1 : request.pointerId,\n              pointerType:'touch',isPrimary:true,width:9,height:9,pressure:0,view:window\n            });\n            if (source) {\n              __cribloSyntheticContextEvents.add(pu);\n              __cribloSyntheticContextSources.set(pu, source);\n            }\n            target.dispatchEvent(pu);\n            __cribloGeoDiag.syntheticPointerUps++;\n            traceAndroidEvent('pointerup', !!(source && source.isTrusted));\n          }\n        } catch (_) {}\n        compatSyntheticPointerDownTarget = null;\n        suppressedIOSPointerUp = null;\n      }`;

swift = replaceOnce(swift, 'exact Android press start', oldEnsure, newEnsure);

swift = replaceOnce(
  swift,
  'trusted synthetic mouse trace',
  `          traceAndroidEvent('mouseup', false);`,
  `          traceAndroidEvent('mouseup', !!(source && source.isTrusted));`,
);
swift = replaceOnce(
  swift,
  'trusted synthetic click trace',
  `          traceAndroidEvent('click', false);`,
  `          traceAndroidEvent('click', !!(source && source.isTrusted));`,
);

swift = replaceOnce(
  swift,
  'release pointer before mouse tail',
  `        // iOS long-presses do not emit Chrome's mouseup/click compatibility\n        // tail. Rebuild only the missing tail, then contextmenu is last.\n        finishAndroidCompatibilityMouse(request, true);`,
  `        // The real iOS pointerup occurred before touchend and was suppressed\n        // from GeoReseaux. Re-emit it now, AFTER touchend, then rebuild the\n        // Android mouseup/click tail. contextmenu remains the final event.\n        finishAndroidPointerRelease(request);\n        finishAndroidCompatibilityMouse(request, true);`,
);

const oldPointerDownObserver = `      __cribloOriginalAddEventListener.call(document, 'pointerdown', function (event) {\n        try {\n          if (!event || !event.isTrusted || String(event.pointerType || '') !== 'touch') return;\n          lastRealPointerDownAt = Date.now();\n          lastRealPointerDownTarget = event.target || null;\n          traceAndroidEvent('pointerdown', true);\n        } catch (_) {}\n      }, true);`;
const newPointerDownObserver = `      __cribloOriginalAddEventListener.call(document, 'pointerdown', function (event) {\n        try {\n          if (!event || !event.isTrusted || String(event.pointerType || '') !== 'touch') return;\n          lastRealPointerDownAt = Date.now();\n          lastRealPointerDownTarget = event.target || null;\n          if (mapLikeTarget(event.target)) {\n            // WKWebView delivers this before touchstart. Hide it from the page;\n            // touchstart will replay the same semantic pointerdown afterwards.\n            suppressedIOSPointerDown = event;\n            event.stopImmediatePropagation();\n            return;\n          }\n          traceAndroidEvent('pointerdown', true);\n        } catch (_) {}\n      }, true);`;
swift = replaceOnce(swift, 'suppress early iOS pointerdown', oldPointerDownObserver, newPointerDownObserver);

const oldPointerUpObserver = `      __cribloOriginalAddEventListener.call(document, 'pointerup', function (event) {\n        try {\n          if (!event || !event.isTrusted || String(event.pointerType || '') !== 'touch') return;\n          lastRealPointerUpAt = Date.now();\n          lastRealPointerUpTarget = event.target || null;\n          traceAndroidEvent('pointerup', true);\n        } catch (_) {}\n      }, true);`;
const newPointerUpObserver = `      __cribloOriginalAddEventListener.call(document, 'pointerup', function (event) {\n        try {\n          if (!event || !event.isTrusted || String(event.pointerType || '') !== 'touch') return;\n          lastRealPointerUpAt = Date.now();\n          lastRealPointerUpTarget = event.target || null;\n          if (mapLikeTarget(event.target) && compatSyntheticPointerDownTarget) {\n            // WKWebView delivers this before touchend. Hide it from GeoReseaux;\n            // touchend will replay pointerup immediately afterwards.\n            suppressedIOSPointerUp = event;\n            event.stopImmediatePropagation();\n            return;\n          }\n          traceAndroidEvent('pointerup', true);\n        } catch (_) {}\n      }, true);`;
swift = replaceOnce(swift, 'suppress early iOS pointerup', oldPointerUpObserver, newPointerUpObserver);

const oldMouseDownObserver = `      __cribloOriginalAddEventListener.call(document, 'mousedown', function (event) {\n        try {\n          if (!event || !event.isTrusted) return;\n          lastRealMouseDownAt = Date.now();\n          lastRealMouseDownTarget = event.target || null;\n          traceAndroidEvent('mousedown', true);\n        } catch (_) {}\n      }, true);`;
const newMouseDownObserver = `      __cribloOriginalAddEventListener.call(document, 'mousedown', function (event) {\n        try {\n          if (!event || !event.isTrusted) return;\n          lastRealMouseDownAt = Date.now();\n          lastRealMouseDownTarget = event.target || null;\n          if (pendingNativeLongPress && pendingNativeLongPress.armed && mapLikeTarget(event.target)) {\n            // A late iOS compatibility mousedown would duplicate the Android\n            // mousedown already replayed after touchstart. Keep it from the page.\n            event.stopImmediatePropagation();\n            return;\n          }\n          traceAndroidEvent('mousedown', true);\n        } catch (_) {}\n      }, true);`;
swift = replaceOnce(swift, 'suppress duplicate iOS mousedown', oldMouseDownObserver, newMouseDownObserver);

const oldMouseUpObserver = `      __cribloOriginalAddEventListener.call(document, 'mouseup', function (event) {\n        try {\n          if (!event || !event.isTrusted) return;\n          lastRealMouseUpAt = Date.now();\n          lastRealMouseUpTarget = event.target || null;\n          traceAndroidEvent('mouseup', true);\n        } catch (_) {}\n      }, true);`;
const newMouseUpObserver = `      __cribloOriginalAddEventListener.call(document, 'mouseup', function (event) {\n        try {\n          if (!event || !event.isTrusted) return;\n          lastRealMouseUpAt = Date.now();\n          lastRealMouseUpTarget = event.target || null;\n          if (pendingNativeLongPress && pendingNativeLongPress.armed && mapLikeTarget(event.target)) {\n            event.stopImmediatePropagation();\n            return;\n          }\n          traceAndroidEvent('mouseup', true);\n        } catch (_) {}\n      }, true);`;
swift = replaceOnce(swift, 'suppress duplicate iOS mouseup', oldMouseUpObserver, newMouseUpObserver);

const oldClickObserver = `      __cribloOriginalAddEventListener.call(document, 'click', function (event) {\n        try {\n          if (!event || !event.isTrusted) return;\n          lastRealClickAt = Date.now();\n          lastRealClickTarget = event.target || null;\n          traceAndroidEvent('click', true);\n\n          var request = pendingNativeLongPress;\n          if (request && request.armed && request.releasedAt && !request.tailCompletionScheduled) {\n            request.tailCompletionScheduled = true;\n            request.tailEvent = event;\n            // We are in capture phase. Run in the next task so GeoReseaux's own\n            // trusted click handlers finish first, then contextmenu is truly last.\n            setTimeout(function () {\n              completeAndroidLongPress(request, 'trusted-click');\n            }, 0);\n          }\n        } catch (_) {}\n      }, true);`;
const newClickObserver = `      __cribloOriginalAddEventListener.call(document, 'click', function (event) {\n        try {\n          if (!event || !event.isTrusted) return;\n          lastRealClickAt = Date.now();\n          lastRealClickTarget = event.target || null;\n          var request = pendingNativeLongPress;\n          if (request && request.armed && mapLikeTarget(event.target)) {\n            // Long-hold release is fully rebuilt below. Suppress any late WebKit\n            // click so GeoReseaux sees exactly one Android-shaped click.\n            event.stopImmediatePropagation();\n            return;\n          }\n          traceAndroidEvent('click', true);\n        } catch (_) {}\n      }, true);`;
swift = replaceOnce(swift, 'suppress duplicate iOS click', oldClickObserver, newClickObserver);

swift = replaceOnce(
  swift,
  'clear pointer state on cancel',
  `        pendingNativeLongPress = null;\n        realTouchTarget = null;`,
  `        pendingNativeLongPress = null;\n        compatSyntheticPointerDownTarget = null;\n        suppressedIOSPointerDown = null;\n        suppressedIOSPointerUp = null;\n        realTouchTarget = null;`,
);

swift = replaceOnce(
  swift,
  'touchend replay pointer for hold and tap',
  `            // The real iPhone trace shows no WebKit mouseup/click for a hold.\n            // Finish the Android compatibility mouse tail in the next task, after\n            // GeoReseaux's genuine touchend handlers have completed. If WebKit\n            // did produce a real click, the capture listener wins and this is a no-op.\n            setTimeout(function () {\n              if (pendingNativeLongPress === request && request.armed && !request.tailCompletionScheduled) {\n                request.tailCompletionScheduled = true;\n                completeAndroidLongPress(request, 'android-compat-tail');\n              }\n            }, 0);\n          } else if (compatSyntheticMouseDownTarget) {\n            var tapCleanup = {\n              target: compatSyntheticMouseDownTarget,\n              x: compatSyntheticMouseDownX,\n              y: compatSyntheticMouseDownY,\n              releaseEvent: event || null,\n              releasedAt: releaseAt,\n              touchStartedAt: lastRealTouchStartAt\n            };\n            setTimeout(function () { finishAndroidCompatibilityMouse(tapCleanup, false); }, 80);\n          }`,
  `            // Run after the genuine touchend has completed propagation. This\n            // produces Android's tail in strict order: pointerup, mouseup, click,\n            // contextmenu. Any early native iOS pointer/mouse tail was suppressed.\n            setTimeout(function () {\n              if (pendingNativeLongPress === request && request.armed && !request.tailCompletionScheduled) {\n                request.tailCompletionScheduled = true;\n                completeAndroidLongPress(request, 'android-compat-tail');\n              }\n            }, 0);\n          } else if (compatSyntheticMouseDownTarget || compatSyntheticPointerDownTarget) {\n            var tapCleanup = {\n              target: compatSyntheticMouseDownTarget || compatSyntheticPointerDownTarget,\n              x: compatSyntheticMouseDownX,\n              y: compatSyntheticMouseDownY,\n              pointerId: realTouchIdentifier,\n              sourceEvent: sourceEvent,\n              releaseEvent: event || null,\n              releasedAt: releaseAt,\n              touchStartedAt: lastRealTouchStartAt\n            };\n            setTimeout(function () { finishAndroidPointerRelease(tapCleanup); }, 0);\n            setTimeout(function () { finishAndroidCompatibilityMouse(tapCleanup, false); }, 80);\n          }`,
);

swift = replaceOnce(
  swift,
  'diagnostic exact order fields',
  `          'Lifecycle facade calls: ' + __cribloGeoDiag.wrappedLifecycleCalls,`,
  `          'Lifecycle facade calls: ' + __cribloGeoDiag.wrappedLifecycleCalls,\n          'iOS pointerdown suppressed: ' + String(!!suppressedIOSPointerDown),\n          'iOS pointerup suppressed: ' + String(!!suppressedIOSPointerUp),`,
);

fs.writeFileSync(swiftPath, swift);

const test = `import { readFileSync } from "node:fs";\n\nconst swiftPath = new URL("../plugins/criblo-native-browser/ios/Sources/CRIBrowserPlugin/CRIBrowserPlugin.swift", import.meta.url);\nconst swift = readFileSync(swiftPath, "utf8");\nconst match = swift.match(/private static let longPressBridgeScript = #\\"\\"\\"([\\s\\S]*?)\\"\\"\\"#/);\nif (!match) throw new Error("iOS long-press bridge script was not found");\n\nclass TestPointerEvent extends Event {\n  constructor(type, init = {}) {\n    super(type, init);\n    for (const [key, value] of Object.entries(init)) {\n      if (["bubbles", "cancelable", "composed"].includes(key)) continue;\n      Object.defineProperty(this, key, { configurable: true, value });\n    }\n  }\n}\nglobalThis.PointerEvent = TestPointerEvent;\nglobalThis.MouseEvent = TestPointerEvent;\nglobalThis.CustomEvent = class extends Event { constructor(type, init = {}) { super(type, init); this.detail = init.detail; } };\nglobalThis.setInterval = () => 1;\nglobalThis.clearInterval = () => {};\nglobalThis.getComputedStyle = () => ({ display: "block", visibility: "visible", opacity: "1" });\n\nconst documentTarget = new EventTarget();\ndocumentTarget.querySelectorAll = () => [];\ndocumentTarget.scripts = [];\ndocumentTarget.parentNode = null;\ndocumentTarget.parentElement = null;\ndocumentTarget.nodeType = 1;\ndocumentTarget.tagName = "CANVAS";\ndocumentTarget.id = "";\ndocumentTarget.className = "";\ndocumentTarget.getBoundingClientRect = () => ({ left: 0, top: 0, width: 200, height: 100 });\ndocumentTarget.closest = () => null;\nconst windowTarget = new EventTarget();\nwindowTarget.window = windowTarget;\nwindowTarget.document = documentTarget;\nwindowTarget.innerWidth = 200;\nwindowTarget.innerHeight = 100;\nwindowTarget.scrollX = 0;\nwindowTarget.scrollY = 0;\nwindowTarget.frames = [];\nwindowTarget.location = { href: "https://sigreseaux.orange.fr/ger/" };\nwindowTarget.getSelection = () => ({ removeAllRanges() {} });\nwindowTarget.__cribloGeoReseauxAndroidCompat = true;\nconst canvas = documentTarget;\ndocumentTarget.elementsFromPoint = () => [canvas];\ndocumentTarget.elementFromPoint = () => canvas;\nglobalThis.window = windowTarget;\nglobalThis.document = documentTarget;\nglobalThis.location = windowTarget.location;\nnew Function(match[1])();\n\nconst order = [];\nconst lifecycle = [];\nlet context = null;\nfunction record(name) {\n  return function (event) {\n    order.push(name);\n    lifecycle.push({ name, trusted: event.isTrusted, touchSource: Boolean(event.sourceCapabilities?.firesTouchEvents), which: event.which });\n  };\n}\nfor (const type of ["pointerdown", "touchstart", "mousedown", "touchend", "pointerup", "mouseup", "click"]) canvas.addEventListener(type, record(type));\ncanvas.addEventListener("contextmenu", (event) => {\n  event.preventDefault();\n  order.push("contextmenu");\n  context = {\n    trusted: event.isTrusted,\n    prevented: event.defaultPrevented,\n    target: event.target === canvas,\n    pointerEvent: event instanceof TestPointerEvent,\n    touchSource: Boolean(event.sourceCapabilities?.firesTouchEvents),\n    clientX: event.clientX, clientY: event.clientY, button: event.button, buttons: event.buttons, pointerType: event.pointerType,\n    originalTrusted: Boolean(event.originalEvent?.isTrusted),\n  };\n});\n\nfunction trusted(event) { Object.defineProperty(event, "isTrusted", { configurable: true, value: true }); return event; }\nfunction pointer(type, x, y, buttons) {\n  canvas.dispatchEvent(trusted(new TestPointerEvent(type, { bubbles:true, cancelable:true, clientX:x, clientY:y, screenX:x, screenY:y, pointerId:9, pointerType:"touch", isPrimary:true, button:0, buttons, pressure:buttons ? 1 : 0 })));\n}\nfunction touch(type, x, y) {\n  const event = trusted(new Event(type, { bubbles:true, cancelable:true }));\n  const point = { identifier:9, target:canvas, clientX:x, clientY:y, pageX:x, pageY:y, screenX:x, screenY:y };\n  const active = type === "touchstart" ? [point] : [];\n  Object.defineProperties(event, { touches:{value:active}, targetTouches:{value:active}, changedTouches:{value:[point]} });\n  canvas.dispatchEvent(event);\n}\nfunction mouse(type, x, y, buttons) {\n  canvas.dispatchEvent(trusted(new TestPointerEvent(type, { bubbles:true, cancelable:true, clientX:x, clientY:y, screenX:x, screenY:y, button:0, buttons })));\n}\nconst wait = (ms) => new Promise((resolve) => setTimeout(resolve, ms));\n\n// Feed the REAL iPhone order. The bridge must hide/reorder it so GeoReseaux\n// listeners see the measured Android order instead.\npointer("pointerdown", 82, 31, 1);\nif (order.length !== 0) throw new Error("early iOS pointerdown leaked to page: " + order.join(","));\ntouch("touchstart", 82, 31);\nawait wait(25);\nif (order.join(",") !== "touchstart,pointerdown,mousedown") throw new Error("Android press order mismatch: " + order.join(","));\nfor (const name of ["pointerdown", "mousedown"]) {\n  const event = lifecycle.find((entry) => entry.name === name);\n  if (!event || !event.trusted || !event.touchSource) throw new Error(name + " trusted-touch facade mismatch: " + JSON.stringify(event));\n}\nawait wait(625);\nif (context !== null) throw new Error("contextmenu fired before release: " + order.join(","));\n\n// iPhone supplies pointerup before touchend. It must be hidden and replayed after\n// touchend. Also inject late native mouse events to prove they cannot duplicate\n// or reorder the controlled Android tail.\npointer("pointerup", 82, 31, 0);\nif (order.join(",") !== "touchstart,pointerdown,mousedown") throw new Error("early iOS pointerup leaked: " + order.join(","));\ntouch("touchend", 82, 31);\nmouse("mousedown", 82, 31, 1);\nmouse("mouseup", 82, 31, 0);\nmouse("click", 82, 31, 0);\nwindowTarget.__cribloNativeWrappedContextLongPress(0.5, 0.5);\nawait wait(45);\nconst expected = "touchstart,pointerdown,mousedown,touchend,pointerup,mouseup,click,contextmenu";\nif (order.join(",") !== expected) throw new Error("PAGE-VISIBLE Android lifecycle mismatch: " + order.join(","));\nfor (const name of ["pointerup", "mouseup", "click"]) {\n  const event = lifecycle.find((entry) => entry.name === name);\n  if (!event || !event.trusted || !event.touchSource) throw new Error(name + " trusted-touch facade mismatch: " + JSON.stringify(event));\n}\nif (!context || !context.trusted || !context.prevented || !context.target || !context.pointerEvent || !context.touchSource || !context.originalTrusted) throw new Error("context facade mismatch: " + JSON.stringify(context));\nif (context.clientX !== 82 || context.clientY !== 31 || context.button !== 0 || context.buttons !== 0 || context.pointerType !== "touch") throw new Error("context shape mismatch: " + JSON.stringify(context));\nif (order.filter((name) => name === "contextmenu").length !== 1) throw new Error("contextmenu duplicated: " + order.join(","));\n\nconst diagnostics = windowTarget.__cribloGeoDiagnosticsText();\nif (!diagnostics.includes("Synthetic press/release: pd=1 md=1 pu=1 mu=1 click=1")) throw new Error("exact Android synthetic counters wrong: " + diagnostics);\nif (!diagnostics.includes("Release completions: 1")) throw new Error("release completion missing: " + diagnostics);\nif (!diagnostics.includes("Android-order trace (*=trusted source): touchstart* > pointerdown* > mousedown* > touchend* > pointerup* > mouseup* > click* > contextmenu*")) throw new Error("diagnostic order is not exact Android: " + diagnostics);\nif (!diagnostics.includes("Lifecycle facade calls: 5")) throw new Error("lifecycle facade count wrong: " + diagnostics);\nif (!diagnostics.includes("Late native bridge suppressed: 1")) throw new Error("late native callback was not suppressed: " + diagnostics);\n\nconsole.log("iOS GeoReseaux page-visible EXACT Android lifecycle test passed");\n`;
fs.writeFileSync(testPath, test);
