import fs from 'node:fs';

const swiftPath = 'plugins/criblo-native-browser/ios/Sources/CRIBrowserPlugin/CRIBrowserPlugin.swift';
const testPath = 'scripts/test-ios-longpress-bridge.mjs';

function replaceOnce(text, label, from, to) {
  const first = text.indexOf(from);
  if (first < 0) throw new Error(`${label}: source block not found`);
  if (text.indexOf(from, first + from.length) >= 0) throw new Error(`${label}: source block not unique`);
  return text.slice(0, first) + to + text.slice(first + from.length);
}

let swift = fs.readFileSync(swiftPath, 'utf8');

swift = replaceOnce(
  swift,
  'dispatch native hold at recognition time',
  `        __cribloGeoDiag.lastResult = 'trusted-webkit-tail-armed-waiting-release';\n        return true;\n      }`,
  `        // The working Android diagnostic shows contextmenu at ~600 ms while\n        // the finger is STILL DOWN, before pointerup/touchend. The previous iOS\n        // bridge waited until physical release (often >1.5 s), which is too late\n        // for GeoReseaux's internal feature-selection state. Dispatch the one\n        // missing contextmenu now, at native recognition time, and clear the\n        // request so release can never emit a duplicate.\n        var before = visiblePopupState();\n        var callsBefore = __cribloGeoDiag.wrappedContextCalls;\n        var dispatched = contextMenu(request.target, request.x, request.y, request.sourceEvent);\n        var delta = __cribloGeoDiag.wrappedContextCalls - callsBefore;\n        __cribloGeoDiag.directTrustedHandlerFires += delta;\n        __cribloGeoDiag.contextAfterTouchMs = lastRealTouchStartAt\n          ? Math.max(0, Date.now() - lastRealTouchStartAt)\n          : -1;\n        pendingNativeLongPress = null;\n        __cribloGeoDiag.lastResult = dispatched\n          ? 'native-hold-contextmenu'\n          : 'native-hold-contextmenu-error';\n        setTimeout(function () {\n          if (popupChanged(before)) __cribloGeoDiag.lastResult = 'native-hold-popup';\n          else if (dispatched) __cribloGeoDiag.lastResult = 'native-hold-no-popup';\n        }, 220);\n        return dispatched;\n      }`,
);

swift = replaceOnce(
  swift,
  'fallback no fake android sequence',
  `        __cribloGeoDiag.lastResult = 'touchstart-timeout-android-sequence';\n        scheduleAndroidTouchFallback(target, x, y);`,
  `        // If WKWebView delays DOM touchstart beyond the native recognizer,\n        // do not fabricate a second touch/pointer/mouse lifecycle. The native\n        // point is already in CSS-point coordinates for this full-screen\n        // WKWebView, so emit the same single hold contextmenu directly.\n        __cribloGeoDiag.lastResult = 'touchstart-timeout-contextmenu-at-hold';\n        var before = visiblePopupState();\n        var callsBefore = __cribloGeoDiag.wrappedContextCalls;\n        var dispatched = contextMenu(target, x, y, null);\n        var delta = __cribloGeoDiag.wrappedContextCalls - callsBefore;\n        __cribloGeoDiag.directTrustedHandlerFires += delta;\n        setTimeout(function () {\n          if (popupChanged(before)) __cribloGeoDiag.lastResult = 'touchstart-timeout-popup';\n          else if (dispatched) __cribloGeoDiag.lastResult = 'touchstart-timeout-no-popup';\n        }, 220);`,
);

swift = replaceOnce(
  swift,
  'native fallback comment/timing',
  `            // The normal delayed WKWebView touchstart arrives within a few\n            // milliseconds. If it never arrives, fall back to a complete\n            // Android-shaped sequence instead of sending contextmenu before\n            // gesture state exists.\n            setTimeout(function () {\n              fallbackNativeLongPressRequest(request);\n            }, 260);`,
  `            // Give a delayed WKWebView touchstart a short opportunity to bind\n            // the genuine target/coordinates. If it still has not arrived, use\n            // the native hold point directly. Do not wait for finger release and\n            // do not fabricate pointer/mouse events.\n            setTimeout(function () {\n              fallbackNativeLongPressRequest(request);\n            }, 90);`,
);

fs.writeFileSync(swiftPath, swift);

const test = `import { readFileSync } from "node:fs";\n\nconst swiftPath = new URL(\n  "../plugins/criblo-native-browser/ios/Sources/CRIBrowserPlugin/CRIBrowserPlugin.swift",\n  import.meta.url,\n);\nconst swift = readFileSync(swiftPath, "utf8");\nconst match = swift.match(/private static let longPressBridgeScript = #\\"\\"\\"([\\s\\S]*?)\\"\\"\\"#/);\nif (!match) throw new Error("iOS long-press bridge script was not found");\n\nclass TestPointerEvent extends Event {\n  constructor(type, init = {}) {\n    super(type, init);\n    for (const [key, value] of Object.entries(init)) {\n      if (["bubbles", "cancelable", "composed"].includes(key)) continue;\n      Object.defineProperty(this, key, { configurable: true, value });\n    }\n  }\n}\nglobalThis.PointerEvent = TestPointerEvent;\nglobalThis.MouseEvent = TestPointerEvent;\nglobalThis.CustomEvent = class extends Event { constructor(type, init = {}) { super(type, init); this.detail = init.detail; } };\nglobalThis.setInterval = () => 1;\nglobalThis.clearInterval = () => {};\nglobalThis.getComputedStyle = () => ({ display: "block", visibility: "visible", opacity: "1" });\n\nconst documentTarget = new EventTarget();\ndocumentTarget.querySelectorAll = () => [];\ndocumentTarget.scripts = [];\ndocumentTarget.parentNode = null;\ndocumentTarget.parentElement = null;\ndocumentTarget.nodeType = 1;\ndocumentTarget.tagName = "CANVAS";\ndocumentTarget.id = "";\ndocumentTarget.className = "";\ndocumentTarget.getBoundingClientRect = () => ({ left:0, top:0, width:200, height:100 });\ndocumentTarget.closest = () => null;\nconst windowTarget = new EventTarget();\nwindowTarget.window = windowTarget;\nwindowTarget.document = documentTarget;\nwindowTarget.innerWidth = 200;\nwindowTarget.innerHeight = 100;\nwindowTarget.scrollX = 0;\nwindowTarget.scrollY = 0;\nwindowTarget.frames = [];\nwindowTarget.location = { href: "https://sigreseaux.orange.fr/ger/" };\nwindowTarget.getSelection = () => ({ removeAllRanges() {} });\nconst canvas = documentTarget;\ndocumentTarget.elementsFromPoint = () => [canvas];\ndocumentTarget.elementFromPoint = () => canvas;\nglobalThis.window = windowTarget;\nglobalThis.document = documentTarget;\nglobalThis.location = windowTarget.location;\nnew Function(match[1])();\n\nlet received = null;\nlet pageOrder = [];\nfunction pageContextHandler(event) {\n  event.preventDefault();\n  pageOrder.push("contextmenu");\n  received = {\n    trusted:event.isTrusted, prevented:event.defaultPrevented, target:event.target===canvas,\n    pointerEvent:event instanceof TestPointerEvent, touchSource:Boolean(event.sourceCapabilities?.firesTouchEvents),\n    clientX:event.clientX, clientY:event.clientY, button:event.button, buttons:event.buttons, pointerType:event.pointerType,\n  };\n}\nfunction trustedEvent(event) { Object.defineProperty(event, "isTrusted", { configurable:true, value:true }); return event; }\nfunction pointer(type,x,y,buttons) { canvas.dispatchEvent(trustedEvent(new TestPointerEvent(type,{bubbles:true,cancelable:true,clientX:x,clientY:y,screenX:x,screenY:y,pointerId:9,pointerType:"touch",isPrimary:true,button:0,buttons,pressure:buttons?1:0}))); }\nfunction mouse(type,x,y,buttons) { canvas.dispatchEvent(trustedEvent(new TestPointerEvent(type,{bubbles:true,cancelable:true,clientX:x,clientY:y,screenX:x,screenY:y,button:0,buttons}))); }\nfunction touch(type,x,y) {\n  const e=trustedEvent(new Event(type,{bubbles:true,cancelable:true}));\n  const t={identifier:9,target:canvas,clientX:x,clientY:y,pageX:x,pageY:y,screenX:x,screenY:y};\n  const active=type==="touchstart"?[t]:[];\n  Object.defineProperties(e,{touches:{value:active},targetTouches:{value:active},changedTouches:{value:[t]}});\n  canvas.dispatchEvent(e);\n}\nconst wait=(ms=15)=>new Promise(r=>setTimeout(r,ms));\nfor (const type of ["pointerdown","touchstart","pointerup","touchend","mousedown","mouseup","click"]) canvas.addEventListener(type,()=>pageOrder.push(type));\ncanvas.addEventListener("contextmenu",pageContextHandler);\n\n// Working Android semantics: hold contextmenu occurs around 600 ms while the\n// finger is still down. It must precede pointerup/touchend.\npointer("pointerdown",82,31,1);\ntouch("touchstart",82,31);\nawait wait(1);\nif (!windowTarget.__cribloNativeWrappedContextLongPress(0.5,0.5)) throw new Error("native hold rejected");\nawait wait(20);\nif (!received) throw new Error(`contextmenu did not fire at native hold: ${pageOrder.join(",")}`);\nif (pageOrder.join(",") !== "pointerdown,touchstart,contextmenu") throw new Error(`hold-time order wrong: ${pageOrder.join(",")}`);\nif (![received.trusted,received.prevented,received.target,received.pointerEvent,received.touchSource].every(Boolean)) throw new Error(`hold facade wrong: ${JSON.stringify(received)}`);\nif (received.clientX!==82 || received.clientY!==31) throw new Error(`hold coordinates wrong: ${JSON.stringify(received)}`);\nif (received.button!==0 || received.buttons!==0 || received.pointerType!=="touch") throw new Error(`context shape wrong: ${JSON.stringify(received)}`);\n\n// Physical release and any later WebKit compatibility tail must not duplicate it.\npointer("pointerup",82,31,0);\ntouch("touchend",82,31);\nmouse("mousedown",82,31,1);\nmouse("mouseup",82,31,0);\nmouse("click",82,31,0);\nawait wait(30);\nif (pageOrder.filter(x=>x==="contextmenu").length!==1) throw new Error(`duplicate contextmenu after release: ${pageOrder.join(",")}`);\nconst expected="pointerdown,touchstart,contextmenu,pointerup,touchend,mousedown,mouseup,click";\nif (pageOrder.join(",")!==expected) throw new Error(`full order wrong: ${pageOrder.join(",")}`);\n\n// Native recognizer may arrive just before DOM touchstart. Once touchstart arrives,\n// it must bind the real point and fire at hold time, not wait for release.\nreceived=null; pageOrder=[];\nwindowTarget.__cribloNativeWrappedContextLongPress(0.5,0.5);\nawait wait(5);\npointer("pointerdown",73,27,1);\ntouch("touchstart",73,27);\nawait wait(20);\nif (!received || received.clientX!==73 || received.clientY!==27) throw new Error(`delayed touch binding failed: ${JSON.stringify(received)}`);\nif (pageOrder.join(",")!=="pointerdown,touchstart,contextmenu") throw new Error(`delayed hold order wrong: ${pageOrder.join(",")}`);\npointer("pointerup",73,27,0); touch("touchend",73,27); await wait(20);\nif (pageOrder.filter(x=>x==="contextmenu").length!==1) throw new Error(`delayed path duplicated menu: ${pageOrder.join(",")}`);\n\nconst diagnostics=windowTarget.__cribloGeoDiagnosticsText();\nif (!diagnostics.includes("Native waits for touchstart: 1")) throw new Error(`native wait missing: ${diagnostics}`);\nif (!diagnostics.includes("Native/touch coordinate delta: 27,23")) throw new Error(`coordinate correction missing: ${diagnostics}`);\nif (!diagnostics.includes("Release completions: 0")) throw new Error(`bridge still waits for release: ${diagnostics}`);\nif (!diagnostics.includes("Synthetic press/release: pd=0 md=0 pu=0 mu=0 click=0")) throw new Error(`fabricated low-level events remain: ${diagnostics}`);\nif (!diagnostics.includes("contextmenu*")) throw new Error(`trusted-source contextmenu trace missing: ${diagnostics}`);\n\ncanvas.removeEventListener("contextmenu",pageContextHandler);\nreceived=null; pageOrder=[];\npointer("pointerdown",60,20,1); touch("touchstart",60,20); await wait(1);\nwindowTarget.__cribloNativeWrappedContextLongPress(0.5,0.5); await wait(20);\nif (received!==null) throw new Error("listener removal identity broken");\nconsole.log("iOS GeoReseaux Android-timed hold bridge test passed");\n`;
fs.writeFileSync(testPath, test);
