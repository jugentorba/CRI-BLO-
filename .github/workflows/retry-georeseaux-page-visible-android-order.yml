name: Retry GeoReseaux page-visible Android order

on:
  push:
    branches:
      - criblo-master-spec-v1
    paths:
      - scripts/fix-georeseaux-page-visible-patch.mjs
      - .github/workflows/retry-georeseaux-page-visible-android-order.yml
  workflow_dispatch:

permissions:
  contents: write

env:
  TARGET_BRANCH: criblo-master-spec-v1

jobs:
  patch-test-build:
    runs-on: macos-15
    timeout-minutes: 50
    steps:
      - name: Checkout working branch
        uses: actions/checkout@v4
        with:
          ref: criblo-master-spec-v1
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Correct temporary patch targeting
        run: node scripts/fix-georeseaux-page-visible-patch.mjs

      - name: Apply page-visible Android order patch
        run: |
          set -euxo pipefail
          node scripts/patch-georeseaux-page-visible-android-order.mjs
          git diff --check

      - name: Install locked dependencies
        run: npm ci --no-audit --no-fund

      - name: Build web assets
        run: npm run build

      - name: Lint source
        run: npm run lint

      - name: Test exact page-visible Android lifecycle
        run: npm run test:ios-longpress-bridge

      - name: Generate iOS project
        run: npx cap add ios

      - name: Sync Capacitor iOS
        run: npx cap sync ios

      - name: Apply CRI-BLO iOS privacy permissions
        run: node scripts/patch-ios-info.mjs

      - name: Build iOS simulator app
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Build unsigned iPhone app
        run: |
          xcodebuild \
            -project ios/App/App.xcodeproj \
            -scheme App \
            -configuration Debug \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$PWD/ios-device-build" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build

      - name: Package and verify unsigned IPA
        run: |
          rm -rf Payload CRI-BLO-iOS-page-visible-android-order.ipa
          mkdir -p Payload
          cp -R ios-device-build/Build/Products/Debug-iphoneos/App.app Payload/App.app
          zip -qry CRI-BLO-iOS-page-visible-android-order.ipa Payload
          unzip -t CRI-BLO-iOS-page-visible-android-order.ipa
          unzip -l CRI-BLO-iOS-page-visible-android-order.ipa | grep -F 'Payload/App.app/Info.plist'

      - name: Commit validated source only
        run: |
          set -euxo pipefail
          rm -f scripts/patch-georeseaux-page-visible-android-order.mjs
          rm -f scripts/fix-georeseaux-page-visible-patch.mjs
          rm -f .github/workflows/apply-georeseaux-page-visible-android-order.yml
          rm -f .github/workflows/retry-georeseaux-page-visible-android-order.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugins/criblo-native-browser/ios/Sources/CRIBrowserPlugin/CRIBrowserPlugin.swift scripts/test-ios-longpress-bridge.mjs scripts/patch-georeseaux-page-visible-android-order.mjs scripts/fix-georeseaux-page-visible-patch.mjs .github/workflows/apply-georeseaux-page-visible-android-order.yml .github/workflows/retry-georeseaux-page-visible-android-order.yml
          git diff --cached --check
          git commit -m "Present exact Android long-press order to GeoReseaux"
          git push origin HEAD:${TARGET_BRANCH}

      - name: Upload validated IPA
        uses: actions/upload-artifact@v4
        with:
          name: CRI-BLO-iOS-page-visible-android-order
          path: CRI-BLO-iOS-page-visible-android-order.ipa
          if-no-files-found: error
